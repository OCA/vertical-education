<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="view_education_student_inherit_admission_form"
            model="ir.ui.view">
            <field name="name">education.student.form</field>
            <field name="model">education.student</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="education.view_education_student_form" />
            <field name="arch" type="xml">
                <xpath expr="//notebook" position="inside">
                    <page string="Fees Collection Details">
                        <field name="fees_detail_ids" readonly="1">
                            <tree string="Fees Collection Details"
                                editable="top">
                                <field name="fees_line_id"
                                    invisible="1" />
                                <field name="date" />
                                <field name="amount" />
                                <field name="product_id"
                                    invisible="1" />
                                <field name="invoice_id"
                                    invisible="1" />
                                <field name="state" />
                                <field name="invoice_state" />
                                <button string="Create Invoice"
                                    states="draft" type="object" name="get_invoice"
                                    groups="education.group_education_back_office" />
                                <button string="View Invoice"
                                    groups="education.group_education_back_office"
                                    states="invoice" type="object"
                                    name="action_get_invoice" />
                            </tree>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
